import { LoaderContainer, LoaderItem } from './Loader.styled';

function Loader() {
    return (
        <LoaderContainer>
            <LoaderItem />
        </LoaderContainer>
    );
}

export default Loader;
